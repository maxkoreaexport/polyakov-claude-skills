# Existing Codex session (optional — or use `init` to create one)
# CODEX_SESSION_ID=sess_your_session_id

CODEX_MODEL=gpt-5.2
CODEX_REASONING_EFFORT=high
CODEX_MAX_ITERATIONS=3
CODEX_YOLO=true

# Custom reviewer prompt (optional — replaces built-in default)
# When set, this prompt is used to initialize the Codex reviewer session.
# Task description and session marker are appended automatically.
# CODEX_REVIEWER_PROMPT="You are a security-focused code reviewer..."
